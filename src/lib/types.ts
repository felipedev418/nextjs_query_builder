export type Query = {
    combinator: string;
    rules: never[];
}

export type Data = {
    firstName: string;
    lastName: string;
    age: number;
    birthdate: Date;
    avatar: string;
    region: string;
    city: string;
}